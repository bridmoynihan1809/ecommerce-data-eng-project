CONFIG = {
    # "orders": {
    #     "watch_directory": "../../src/landing/orders",
    #     "is_daemon": True,
    # }
    # ,
    "customers": {
        "watch_directory": "../../src/landing/customers",
        "is_daemon": True,
    }
}